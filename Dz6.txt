№1А)
Проанализировав предыдущие задания я нашёл 2 случая где нужна была передача указателя:
1) в 4 задании в массиве в строках


func InsertionSort(ar []int) {
	for i := 1; i < len(ar); i++ {
		x := ar[i]
		j := i
		for ; j >= 1 && ar[j-1] > x; j-- {
			ar[j] = ar[j-1]
		}
		ar[j] = x
	} 

Х мог бы стать указателем, в качестве входного параметра и возвращаемого результата

2) в задании 5 я пытался использовать передачу указателя (В 2 моментаах) но провалился и решил не усложнять себе жизнь, но в теории оно должно было работать так:

Значение [i] использовался бы как указатель  входного параметра и возвращаемого результата, т.к используется он очень много и ячейка упростила бы его использование.

№1Б)
2)Например когда мы используем .int, часто он является указателем передачи параметров.  






№2)
Когда " * "  используется перед типом переменной и перед самой переменной var, в таких случаях воспринимается как указатель. 
В других же случаях использования звёздочки она будет восприниматься как арифм.действие. (Если использование её уместное)
